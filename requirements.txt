# Vaibhav Files Transfer - Python dependencies
# Works on Windows, Mac, Linux, and Render.com
# NOTE: pyzbar removed — it needs a native zbar DLL on Windows that pip
# cannot install. The app uses a .url sidecar file instead (no DLL needed).

Flask==3.0.3
Flask-SocketIO==5.3.6
python-socketio==5.11.4
python-engineio==4.9.1
eventlet==0.36.1
qrcode==7.4.2
Pillow==10.4.0
requests==2.32.3
